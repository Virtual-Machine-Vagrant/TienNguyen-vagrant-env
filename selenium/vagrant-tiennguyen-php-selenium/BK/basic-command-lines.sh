Stopping and starting Apache once PHP is installed

# /etc/init.d/apache2 stop
# /etc/init.d/apache2 start

#=========================================================

Create an alias for the Selenium Server

# edit ~/.bash_profile
Add this line into the file:
alias selenium="java -jar /usr/local/bin/selenium-server-standalone-3.0.0.jar"

Start Selenium
# $ selenium
#=========================================================